{
  "name": "control-de-gastos",
  "version": "1.0.0",
  "private": true,
  "description": "Aplicacion de control de gastos - monorepo (backend + front)",
  "scripts": {
    "dev:backend": "pnpm --filter backend run dev",
    "dev:front": "pnpm --filter front run start",
    "build:backend": "pnpm --filter backend run build",
    "build:front": "pnpm --filter front run build"
  },
  "devDependencies": {
    "typescript": "~5.5.4"
  },
  "overrides": {
    "typescript": "~5.5.4"
  }
}